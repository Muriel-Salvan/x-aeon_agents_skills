#!/usr/bin/env ruby

# Parameters:
# * *github_issue_number* (Integer): The Github issue number to implement
# * *run_id* (String): [Optional] The run ID for persistence
github_issue_number = Integer(ARGV[0])
run_id = ARGV[1]

require 'x-aeon_agents_skills/agents'

XAeonAgentsSkills::Agents.configure(
  cline_api_key: Bundler.with_unbundled_env { `launcher safe -- "Cline API key"`.match(/\[PASSWORD\][^:]+: ([^\n]+)\n/)[1] },
  debug: true
)

XAeonAgentsSkills::Agents.implement_requirements(`gh issue view #{github_issue_number} --json=number,title,body,comments,labels,state,url`, run_id:, commit: true)
