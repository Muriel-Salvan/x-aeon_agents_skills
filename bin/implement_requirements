#!/usr/bin/env ruby

# Parameters:
# * *requirements* (String): The requirements to implement
requirements = ARGV[0]

require 'x-aeon_agents_skills/agents'

XAeonAgentsSkills::Agents.configure(
  cline_api_key: Bundler.with_unbundled_env { `launcher safe -- "Cline API key"`.match(/\[PASSWORD\][^:]+: ([^\n]+)\n/)[1] },
  debug: true
)

XAeonAgentsSkills::Agents.implement_requirements(requirements)
